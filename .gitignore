# Rust
crate/target/

# build artifacts (root dev build only; per-OS release plugins live in ado/)
build/*.o
build/*.plugin
/fastm.plugin

# Stata
*.smcl
*.log

# OS
.DS_Store

# parity fixtures (regenerate via tests/parity/export_poliblog.R)
tests/parity/*.dta

# internal planning (not part of the public package)
docs/

# local cargo override for engine dev (not for distribution)
crate/.cargo/

# replication package log ships with the SJ submission
!replication/*.log

# Zotero reference library (local only; 41MB of source PDFs, not part of the package)
zotero/
