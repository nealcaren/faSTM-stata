# Rust
crate/target/

# build artifacts (root dev build only; per-OS release plugins live in ado/)
build/*.o
build/*.plugin
/fastm.plugin

# Stata
*.smcl
*.log

# OS
.DS_Store

# local cargo override for engine dev (not for distribution)
crate/.cargo/

# Work products kept local, out of the install-facing repo (paper, drafts,
# article replication, R-stm parity fixtures, internal design/planning notes,
# the submission bundle, and the Zotero reference library).
docs/
submission/
zotero/
DESIGN.md
/replication/
/tests/parity/
